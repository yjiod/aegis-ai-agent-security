# 安全响应

请在 GitHub 私有仓库创建仅成员可见的安全通告或直接联系仓库所有者。不要在普通 Issue 中提交令牌、客户地址、设备标识、报告正文或利用样本。

报告应包含受影响版本、最小复现、影响边界和建议修复。凭据泄露、任意代码执行和越界写入优先级最高；扫描绕过、报告伪造和不安全厂商动作次之。

疑似泄露的 Collector、HMAC、深信服或联软凭据必须立即轮换，并检查 Collector 审计、反向代理日志和厂商操作审计。禁止把新凭据写入 Git 历史。
