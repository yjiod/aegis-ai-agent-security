# privacy-blocklist.local.example — 本地明文 substring 扫描层的格式样例（可入仓，仅占位）。
#
# 用法：复制为 scripts/privacy-blocklist.local（已 gitignore，绝不入仓/推送），把占位换成
#       你环境里真实需要拦截的敏感字面量 ERE 正则。维护者私存，不进版本库。
#
# 格式：每行  类别<TAB>ERE正则   （制表符分隔；正则内可含 | ( ) 等，不要含制表符）
# 以 # 开头的行与空行忽略。privacy-scan.sh 层 2 会逐行 git grep -E 这些正则（排除扫描器自身）。
#
# 为什么需要它：CI 只跑「通用结构模式 + HMAC 指纹」两层，指纹按独立 token 比对，抓不到嵌进
# 更长单词的拉丁字面量（如 vendornamecorp）。本地这层用明文 substring 正则补齐——推送前本地
# 必跑 privacy-scan.sh，故真实字面量在进 CI/上 GitHub 之前就被拦下。
#
# 类别建议：vendor / personal / domain / employee / device
vendor	([Vv]endorPlaceholder|占位厂商名)
personal	(your_username|YourMachineName|真实姓名占位)
domain	(console\.yourdomain|\.yourdomain\.com)
employee	(00000000)
device	(SERIALPLACEHOLDER|deviceidhexplaceholder)
